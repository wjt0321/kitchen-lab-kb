# 样品库知识库管理系统 — 设计文档 v2 (定稿)

> **状态**: ✅ 定稿(功能层 + UI/UX 视觉层)
> **作者**: 旺财 🦅(功能层)+ 小黑 🐱(UI/UX 视觉层)
> **日期**: 2026-06-05
> **后续**: 教主拿这份文档自己操作,无需任何代码补充

---

## Changelog

| 版本 | 日期 | 内容 | 作者 |
|---|---|---|---|
| v1 | 2026-06-05 | 功能/数据/流程层初稿 | 旺财 |
| **v2** | **2026-06-05** | **整合小黑 UI/UX 视觉层反馈,设计定稿** | **旺财** |
| **v2.1** | **2026-06-07** | **筛选栏 UI/UX 重构:字段分组、响应式网格、查询动作区统一** | **Codex** |

> v2 是**自包含**的定稿文档,不需要回看 v1。v1.md 保留作为过程记录。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型](#2-技术选型)
3. [数据模型](#3-数据模型)
4. [核心算法](#4-核心算法)
5. [功能模块](#5-功能模块)
6. [页面流程与设计](#6-页面流程与设计)
7. [设计系统](#7-设计系统)
8. [组件规范](#8-组件规范)
9. [交互细节](#9-交互细节)
10. [状态反馈(空/加载/错误)](#10-状态反馈空加载错误)
11. [桌面窗适配](#11-桌面窗适配)
12. [API 设计](#12-api-设计)
13. [非功能性需求](#13-非功能性需求)
14. [后续扩展位](#14-后续扩展位)
15. [MVP 实施优先级](#15-mvp-实施优先级)
16. [关键设计决策清单](#16-关键设计决策清单)
17. [附录](#17-附录)

---

## 1. 项目概述

### 1.1 目标
为研发人员提供一个**轻量、易迁移**的样品库知识库管理系统,记录:
- 产品主数据(品号/规格/品名/数量)
- 每次研发试验(原料/辅料/配方/成功或失败)
- 配方组合的成功率(支持迭代优化查询)

### 1.2 范围(MVP)
- 产品主数据 CRUD
- 配方试验记录 CRUD(每次试验一条)
- 原料/辅料录入
- 配方组合 hash 自动识别同一配方
- 配方成功率查询
- Excel 导出(产品/配方/成功率)
- 一键备份(.db 打包)
- JSON 全量导出
- 简单登录(只记用户名,不做密码)
- 归档(软删除)

### 1.3 用户
- **MVP 阶段**:1 个主要研发人员
- **后续扩展**:多人协作(数据模型先留 `user_id` 字段)

### 1.4 核心约束
| 约束 | 体现 |
|---|---|
| Windows 环境 | PyWebView 桌面窗,内嵌 FastAPI |
| 避免过多依赖 | 4 个 pip 包(fastapi/uvicorn/pywebview/openpyxl)+ stdlib |
| 多电脑易迁移 | 整个文件夹 + `pip install -r requirements.txt` + `python startup.py` |
| 简单记录/查询/修改/删除/归档 | 不引前端框架,原生 HTML/CSS/JS |

---

## 2. 技术选型

| 组件 | 选择 | 理由 |
|---|---|---|
| 后端 | **FastAPI** | 异步、自动文档、轻量 |
| 数据库 | **SQLite** (stdlib `sqlite3`) | 单文件、零依赖、易迁移 |
| ORM | **不用** | 表少(4 张),直接 SQL 更易读 |
| 前端 | **原生 HTML/CSS/JS** | 不引框架,保持轻 |
| 模板 | **stdlib `string.Template`** | 不引 Jinja2(又少一依赖) |
| 桌面壳 | **PyWebView** | 把 FastAPI 嵌进 Win 窗口 |
| Excel | **openpyxl** | 唯一 Excel 库,格式丰富 |
| 备份 | **stdlib `zipfile`** | 零依赖 |
| HTTP 启动 | **uvicorn** | FastAPI 标准搭档 |

**最终依赖只有 4 个**:`fastapi / uvicorn / pywebview / openpyxl`

---

## 3. 数据模型

### 3.1 表结构

```sql
-- 产品主表
CREATE TABLE products (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    品号            TEXT NOT NULL UNIQUE,       -- 产品编号(主键索引)
    规格            TEXT NOT NULL,              -- 规格
    品名            TEXT NOT NULL,              -- 品名
    当前数量        INTEGER DEFAULT 0,         -- 当前数量(可手动调整/自动累计用量)
    备注            TEXT,                       -- 备注
    状态            TEXT DEFAULT 'active',      -- active / archived
    created_by      TEXT,                       -- 登录用户名(留扩展位)
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 配方/试验记录(每一次试验一条)
CREATE TABLE recipes (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    产品id          INTEGER NOT NULL,           -- FK → products.id
    试验日期        DATE NOT NULL,
    配方名称        TEXT,                       -- 可选(如"配方A"/"实验-001")
    配方hash        TEXT NOT NULL,              -- 配方组合 hash(见 §4)
    状态            TEXT NOT NULL,              -- success / failed / pending
    用了多少        INTEGER,                    -- 本次试验用了多少
    备注            TEXT,
    created_by      TEXT,                       -- 登录用户名
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (产品id) REFERENCES products(id)
);

-- 原料/辅料(一条配方可多条)
CREATE TABLE recipe_materials (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    配方id          INTEGER NOT NULL,           -- FK → recipes.id
    类型            TEXT NOT NULL,              -- 原料 / 辅料
    名称            TEXT NOT NULL,
    用量            REAL NOT NULL,
    单位            TEXT,                       -- g / kg / ml / 颗 ...
    排序            INTEGER DEFAULT 0,          -- 录入顺序
    FOREIGN KEY (配方id) REFERENCES recipes(id)
);

-- 登录记录(简单登录用,不做密码校验)
CREATE TABLE user_logins (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    用户名          TEXT NOT NULL,
    登录时间        TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    登出时间        TIMESTAMP
);
```

### 3.2 索引
```sql
CREATE INDEX idx_products_品号 ON products(品号);
CREATE INDEX idx_products_状态 ON products(状态);
CREATE INDEX idx_recipes_产品id ON recipes(产品id);
CREATE INDEX idx_recipes_配方hash ON recipes(配方hash);
CREATE INDEX idx_recipes_状态 ON recipes(状态);
CREATE INDEX idx_recipes_试验日期 ON recipes(试验日期);
CREATE INDEX idx_materials_配方id ON recipe_materials(配方id);
```

### 3.3 关键决策
- **`配方hash` 是计算字段,新建配方时自动算出来写入** — 查询时不用每次重算
- **`created_by` 字段**:MVP 阶段只填值(当前登录用户名),不做权限校验;后续要做审计直接查
- **软删除**:归档 = `状态='archived'`,查询时默认 filter 掉
- **数量字段**:只记"用了多少",存在 `recipes.用了多少`;产品主表的 `当前数量` 是**冗余字段**,可手动改/后续自动累计(留扩展位)

---

## 4. 核心算法

### 4.1 配方组合 Hash

**目的**:识别"同一配方"的不同试验记录,计算该配方的成功率

**算法**:
```python
import hashlib
import json

def recipe_hash(品号, 原料列表, 辅料列表):
    """
    原料/辅料列表: [(名称, 用量, 单位), ...]
    """
    # 1. 排序(确保相同组合 → 相同 hash)
    原料_sorted = sorted(原料列表, key=lambda x: x[0])
    辅料_sorted = sorted(辅料列表, key=lambda x: x[0])

    # 2. 标准化为字符串
    payload = {
        "品号": 品号,
        "原料": [{"名称": n, "用量": q, "单位": u} for n, q, u in 原料_sorted],
        "辅料": [{"名称": n, "用量": q, "单位": u} for n, q, u in 辅料_sorted],
    }

    # 3. 序列化 + sha256
    raw = json.dumps(payload, ensure_ascii=False, sort_keys=True).encode("utf-8")
    return hashlib.sha256(raw).hexdigest()[:16]  # 取前 16 位
```

**示例**:
- 配方 A: 品号 P001 + 原料[水 100ml, 糖 50g] + 辅料[香精 1ml] → hash = `a1b2c3d4e5f6g7h8`
- 同配方 A 重做:hash 不变,关联到同一组合
- 配方 B(改了一个用量):hash 变 → 新配方

### 4.2 成功率计算

**公式**:
```
配方组合成功率 = 该 hash 下 status='success' 的记录数 / 该 hash 下总记录数
```

**查询示例**:
```sql
-- 按"产品"看所有去重配方 + 成功率
SELECT 配方hash,
       COUNT(*) as 试验次数,
       SUM(CASE WHEN 状态='success' THEN 1 ELSE 0 END) as 成功次数,
       ROUND(SUM(CASE WHEN 状态='success' THEN 1 ELSE 0 END) * 1.0 / COUNT(*), 2) as 成功率
FROM recipes
WHERE 产品id = ? AND 状态 IN ('success', 'failed')
GROUP BY 配方hash
ORDER BY 成功率 DESC;
```

**视图(预定义,UI 调用方便)**:
```sql
CREATE VIEW v_recipe_success_rate AS
SELECT 产品id, 配方hash,
       COUNT(*) as 试验次数,
       SUM(CASE WHEN 状态='success' THEN 1 ELSE 0 END) as 成功次数,
       ROUND(SUM(CASE WHEN 状态='success' THEN 1 ELSE 0 END) * 1.0 / COUNT(*), 2) as 成功率
FROM recipes
WHERE 状态 IN ('success', 'failed')
GROUP BY 产品id, 配方hash;
```

---

## 5. 功能模块

### 5.1 登录
- **输入**:用户名(纯文本,无密码)
- **行为**:
  - 写入 `user_logins` 表(用户名 + 登录时间)
  - 存到前端 localStorage + 后端 session cookie(简单实现)
- **MVP 约束**:不做密码,不做权限,只记录"谁在线"
- **登出**:更新 `登出时间`

### 5.2 产品 CRUD
- **新增**:品号(必填/唯一)+ 规格 + 品名 + 数量(可空) + 备注
- **查询**:列表(支持分页,默认按创建时间倒序)
- **编辑**:所有字段可改
- **删除/归档**:`状态='archived'`,列表默认隐藏,有"显示已归档"开关
- **详情**:产品基本信息 + 该产品下的所有配方记录 + 配方组合成功率

### 5.3 配方 CRUD
- **新增**:产品(下拉选) + 试验日期 + 配方名称(可空) + 原料/辅料(动态行) + 用了多少 + 状态(success/failed/pending) + 备注
- **系统自动算**:`配方hash`(提交时算)
- **查询**:列表(支持按产品/状态/日期范围筛选)
- **编辑**:所有字段可改(改原料 → hash 重算)
- **删除**:真删(配方是试验记录,删了正常)

### 5.4 原料/辅料录入
- **交互**:动态表格(可加行/删行)
- **字段**:类型(原料/辅料)+ 名称 + 用量 + 单位
- **常用项**:暂不做下拉(后续可加);单位用 datalist 提供建议

### 5.5 搜索与筛选
- **产品搜索**:品号/品名 模糊匹配(`LIKE '%xxx%'`)
- **配方搜索**:按产品 + 状态 + 日期范围 + 成功率范围
- **结果排序**:可按字段排序(品号/创建时间/成功率)

### 5.6 归档
- **产品归档**:`状态='archived'`
- **查看归档**:列表上方"显示已归档"开关
- **恢复归档**:`状态='active'`

### 5.7 Excel 导出
- **导出产品列表**:`品号 / 规格 / 品名 / 当前数量 / 状态 / 创建时间`
- **导出配方列表**:`产品id / 品号 / 试验日期 / 配方名称 / 状态 / 用了多少 / 备注`
- **导出配方组合成功率**:`产品 / 配方hash / 试验次数 / 成功次数 / 成功率`
- **格式**:xlsx(可指定文件名,带时间戳)
- **路径**:用户选保存路径

### 5.8 一键备份
- **行为**:把 `data/kitchen.db` 打包成 `backups/kitchen_YYYYMMDD_HHMMSS.zip`
- **保留策略**:默认保留最近 10 个备份,超出自动删最老的(后续加)
- **触发**:UI 顶栏"备份"按钮(二次确认)

### 5.9 JSON 导出
- **行为**:导出全部数据为 JSON(产品/配方/原料辅料),便于跨系统迁移
- **格式**:
  ```json
  {
    "exported_at": "2026-06-05T18:30:00",
    "products": [...],
    "recipes": [...],
    "materials": [...]
  }
  ```

---

## 6. 页面流程与设计

### 6.0 整体视觉基调(小黑定稿)

> **轻量桌面管理台 + 文档式详情页**
> 关键词:简单、清楚、研发习惯、表格优先、状态可读、少装饰、少弹窗、高容错

**参考对象**(混合体):
- **GitHub**:列表、状态标签、详情页信息层级
- **Notion Database**:表格 + 详情页 + 空状态
- **飞书多维表格**:筛选器、按钮、轻量卡片
- **石墨文档**:简洁、低视觉噪音

**不要**:
- 大面积渐变
- 复杂动效
- 花哨插画
- 重型侧边栏
- 悬浮按钮
- 过多颜色
- 过多 emoji

**最重要的三个设计决策**:
1. **不要侧边栏,顶部导航足够**
2. **产品详情页用 Tabs 分开"配方记录"和"成功率汇总"**
3. **新建配方页必须分块(否则动态原料/辅料表格会显得混乱)**

### 6.1 全局布局

**采用顶部导航 + 主内容区**(不要侧边栏):
- 功能模块少,侧边栏显重
- 1024×768 窗口宽度有限,侧边栏会压缩表格
- 研发人员最常用路径:产品列表 → 产品详情 → 配方详情/新建配方,顶部导航足够

**全局结构**:
```
┌────────────────────────────────────────────────────────────┐
│ 顶栏(48px)│ 样品库知识库 │ 产品 │ 成功率查询 │  用户名 备份 导出 退出 │
├────────────────────────────────────────────────────────────┤
│ 面包屑 / 页面标题 / 主操作按钮                              │
├────────────────────────────────────────────────────────────┤
│ 搜索 / 筛选 / 状态切换                                      │
├────────────────────────────────────────────────────────────┤
│ 主内容:表格 / 表单 / 详情卡片                                │
├────────────────────────────────────────────────────────────┤
│ 分页 / 保存区 / 状态提示                                    │
└────────────────────────────────────────────────────────────┘
```

**内容区宽度**:`padding: 20px 24px; max-width: none;`(表格需要横向空间,不要限制 960px)

### 6.2 登录页

**元素**:
- 居中卡片(360px 宽),背景 `#F6F8FA`
- 系统标题"样品库知识库" + 副标题"研发样品与配方记录工具"
- 用户名输入框 + "登录"按钮
- (可选)上次登录用户名显示

**线框**:
```
┌──────────────────────────────┐
│ 样品库知识库                  │
│ 研发样品与配方记录工具         │
│                              │
│ 用户名                        │
│ [                            ]│
│                              │
│ [登录]                        │
│                              │
│ 上次登录:wangcai              │
└──────────────────────────────┘
```

**跳转**:登录成功 → `/products`(产品列表)

### 6.3 产品列表页(首页)

**路径**:`/products`

**元素**(由上到下):
- 顶栏(全局)
- **页面标题区**:面包屑 + "产品列表" + 副标题"管理产品主数据、库存数量和配方记录" + 主按钮"[+ 新建产品]"
- **筛选区**:独立筛选面板,包含面板标题、结果数量、字段标签、关键词搜索、状态下拉、查询/重置动作区
- **产品列表表格**(列宽建议):
  - 品号(110px) / 品名(自适应) / 规格(120px) / 当前数量(90px) / 状态(80px) / 配方数(80px) / 创建时间(140px) / 操作(120px)
- **操作列**:查看 | 编辑 | 归档(归档弱化颜色)
- **分页**:底部右侧 "共 N 条 上一页 第 X 页 下一页"
- 表格行 hover 高亮 `background: #F6F8FA; cursor: pointer;`
- 整行可点击进入详情,品号做成蓝色链接(两种习惯都兼容)
- 操作列点击时阻止行跳转

**线框**:
```
┌────────────────────────────────────────────────────────────┐
│ 产品列表                                      [+ 新建产品] │
│ 管理产品主数据、库存数量和配方记录                         │
├────────────────────────────────────────────────────────────┤
│ 筛选产品                                           共 N 条 │
│ 关键词 [输入品号或品名                    ] 状态 [活跃 v] │
│                                      [查询] [重置]         │
├────────────────────────────────────────────────────────────┤
│ 品号     品名            规格      当前数量  状态    配方数  操作 │
│ P001     柠檬饮料基底     500ml     120       活跃    8      查看 │
│ P002     草莓风味样品     1kg       40        活跃    3      查看 │
│ P003     老版配方样品     250ml     0         已归档  12     恢复 │
├────────────────────────────────────────────────────────────┤
│ 共 128 条                                      < 1 2 3 >   │
└────────────────────────────────────────────────────────────┘
```

**空状态**(无产品):
```
暂无产品
还没有录入任何产品。先新建一个产品,再记录配方试验。
[+ 新建产品]
```

**空状态**(搜索无结果):
```
没有找到匹配的产品
请尝试更换关键词,或清空筛选条件。
[清空筛选]
```

### 6.4 产品详情页

**路径**:`/products/{id}`

**结构**:**产品信息卡 + 下方 Tabs**

**线框**:
```
产品列表 / P001

┌────────────────────────────────────────────────────────────┐
│ P001  柠檬风味饮料基底                         [新建配方]  │
│ 规格:500ml    当前数量:120    状态:活跃                    │
│ 备注:用于饮料样品测试……                       [编辑] [归档] │
└────────────────────────────────────────────────────────────┘

Tabs:  配方记录  |  成功率汇总

┌────────────────────────────────────────────────────────────┐
│ 筛选配方记录                                  N 条记录     │
│ 结果状态 [全部状态 v] 原料/辅料 [输入原料或辅料名称]       │
│ 试验日期 [2026-01-01] 至 [2026-06-05]                     │
│ 成功率 [最低] % [最高] %   排序 [试验日期 v] [降序 v]     │
│                                      [查询] [重置]         │
├────────────────────────────────────────────────────────────┤
│ 日期        配方名称       状态      用量    创建人   操作    │
│ 2026-06-05  配方A-第三版    成功      20g     wangcai  查看   │
│ 2026-06-01  配方A-第二版    失败      20g     wangcai  查看   │
└────────────────────────────────────────────────────────────┘
```

**Tabs 规范**:
- 当前 Tab 蓝色文字 + 下边框蓝色
- 不用大按钮式 Tab
- 默认 Tab:**配方记录**
- 第二 Tab:**成功率汇总**

**空状态**(无配方):
```
暂无配方记录
这个产品还没有试验记录。可以新建一条配方,记录原料、辅料和试验结果。
[+ 新建配方]
```

### 6.5 新建/编辑产品页

**路径**:`/products/new` / `/products/{id}/edit`

**线框**(窄表单卡片,`max-width: 680px`):
```
产品列表 / 新建产品

┌──────────────────────────────────────┐
│ 基本信息                              │
│ 品号 *       [ P001                 ] │
│ 品名 *       [ 柠檬风味饮料基底      ] │
│ 规格 *       [ 500ml                ] │
│ 当前数量     [ 120                  ] │
│ 备注         [ 多行文本              ] │
│                                      │
│                          [取消] [保存] │
└──────────────────────────────────────┘
```

**按钮位置**:底部右侧 — 取消(次级)+ 保存(主)
**不用浮动保存按钮**(桌面管理系统不需要)

### 6.6 新建/编辑配方页

**路径**:`/recipes/new?product_id=xxx` / `/recipes/{id}/edit`

**这是全系统最复杂页面,必须分 3 块**:

1. **试验信息**(块 1)
2. **原料/辅料动态表格**(块 2)
3. **状态与备注**(块 3)

**线框**:
```
产品列表 / P001 / 新建配方

┌────────────────────────────────────────────────────────────┐
│ 试验信息                                                    │
│ 产品       [P001 柠檬风味饮料基底 v]   试验日期 [2026-06-05] │
│ 配方名称   [配方A-第三版             ]   用了多少 [20    ]   │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ 原料 / 辅料                                        [+ 添加行]│
│ 类型      名称              用量             单位       操作  │
│ [原料 v]  [水             ] [100          ] [ml v]    删除  │
│ [原料 v]  [糖             ] [50           ] [g  v]    删除  │
│ [辅料 v]  [香精           ] [1            ] [ml v]    删除  │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ 结果状态                                                    │
│ ○ 成功    ○ 失败    ○ 待观察                                │
│ 备注                                                        │
│ [多行文本                                                  ] │
│                                                            │
│                                      [取消] [保存配方]       │
└────────────────────────────────────────────────────────────┘
```

**单位字段**:`<input list="unit-list">` + datalist(不引库)
```html
<datalist id="unit-list">
  <option value="g"><option value="kg"><option value="ml">
  <option value="L"><option value="颗">
</datalist>
```

**动态表格交互规则**:
- 默认至少 1 行原料
- "添加行"放表格右上
- 每行最后一列"删除"
- 删除行无需二次确认
- 保存时:忽略完全空白行,部分填写的行提示"第 N 行缺少'用量'"

### 6.7 配方详情页

**路径**:`/recipes/{id}`

**采用文档式详情页,不要做成表格堆叠**

**线框**:
```
产品列表 / P001 / 配方A-第三版

┌────────────────────────────────────────────────────────────┐
│ 配方A-第三版                                  [编辑] [删除] │
│ 状态:成功   日期:2026-06-05   用量:20g   创建人:wangcai    │
│ Hash:a1b2c3d4e5f6g7h8  [复制]                              │
└────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│ 原料                         │
│ 水       100 ml              │
│ 糖       50 g                │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 辅料                         │
│ 香精     1 ml                │
└──────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ 同组合试验记录                                               │
│ 当前组合成功率:66.7%   试验 3 次 / 成功 2 次                │
│ 2026-06-05  成功                                             │
│ 2026-06-01  失败                                             │
│ 2026-05-28  成功                                             │
└────────────────────────────────────────────────────────────┘
```

### 6.8 成功率查询页

**路径**:`/success-rate`(可作为产品详情 Tab,也可独立)

**线框**:
```
成功率查询

┌────────────────────────────────────────────────────────────┐
│ 筛选成功率                                  N 组配方       │
│ 产品 [全部产品 v]  试验日期 [起始] 至 [结束]  结果 [成功/失败] │
│ 成功率区间 [最低] % [最高] %  最少试验 [次数]              │
│ 排序 [成功率 v] [降序 v]                    [查询] [重置]  │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ 产品       配方组合       试验次数   成功次数   成功率       操作 │
│ P001       a1b2...g7h8    12         9          ███████░ 75% 展开 │
│ P002       b2c3...h8i9    4          1          ██░░░░░░ 25% 展开 │
└────────────────────────────────────────────────────────────┘
```

**点击 hash 或"展开"** → 展示该组合下的全部试验

**空状态**:
```
暂无成功率数据
至少需要一条"成功"或"失败"的配方记录,才能计算成功率。
待观察状态不会参与成功率计算。
```

---

## 7. 设计系统

### 7.1 色彩

**基础色**(冷静的实验室管理风格):
| 用途 | 颜色 | 说明 |
|---|---|---|
| 页面背景 | `#F6F8FA` | 类 GitHub 背景,耐看 |
| 卡片背景 | `#FFFFFF` | 内容区域 |
| 边框 | `#D8DEE4` | 表格、卡片、输入框 |
| 主文字 | `#24292F` | 高可读 |
| 次级文字 | `#57606A` | 说明、meta 信息 |
| 弱文字 | `#8C959F` | placeholder、辅助 |

**功能色**:
| 用途 | 颜色 | 说明 |
|---|---|---|
| 主色 | `#2563EB` | 蓝色,可靠、熟悉 |
| 主色 hover | `#1D4ED8` | 按钮 hover |
| 成功 | `#16A34A` | success |
| 失败/危险 | `#DC2626` | failed / 删除 |
| 警告 | `#D97706` | pending / 备份警告 |
| 归档/禁用 | `#6B7280` | archived |
| 信息 | `#0891B2` | 备份、导出成功等 |

**状态浅底色**(用于 Badge 背景):
| 状态 | 文字 | 背景 |
|---|---|---|
| success | `#15803D` | `#DCFCE7` |
| failed | `#B91C1C` | `#FEE2E2` |
| pending | `#B45309` | `#FEF3C7` |
| active | `#1D4ED8` | `#DBEAFE` |
| archived | `#4B5563` | `#F3F4F6` |

### 7.2 字体

**字体栈**(Windows 优先):
```css
font-family:
  "Segoe UI",
  "Microsoft YaHei",
  "PingFang SC",
  system-ui,
  -apple-system,
  sans-serif;
```

**字号**:
| 用途 | 字号 |
|---|---:|
| 页面标题 | 20px |
| 区块标题 | 16px |
| 正文/表格 | 14px |
| 辅助文字 | 13px |
| 表单错误 | 12px |
| 徽章 | 12px |

**行高**:
```css
line-height: 1.5;  /* 正文 */
line-height: 1.4;  /* 表格 */
```

### 7.3 圆角与阴影(整体克制)

```css
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
```

**卡片**:
```css
border: 1px solid #D8DEE4;
border-radius: 8px;
box-shadow: none;  /* 不要重阴影 */
```

### 7.4 间距

- 页面内容区 padding: `20px 24px`
- 卡片间距 margin-bottom: `16px`
- 表格行高: `44px`
- 单元格横向 padding: `12px`
- 区块标题与内容间距: `16px`

### 7.5 图标风格

**MVP 不建议引入大型图标库**。优先级:
1. 纯文字按钮(`+ 新建产品` `导出` `备份` `编辑` `归档` `删除`)
2. 少量 Unicode 符号(`+` `←` `…`)
3. 必要时使用内联 SVG

**不建议大量 emoji**

---

## 8. 组件规范

### 8.1 输入框/文本域

```css
.input {
  height: 34px;
  padding: 6px 10px;
  border: 1px solid #D8DEE4;
  border-radius: 6px;
  font-size: 14px;
}

.input:focus {
  border-color: #2563EB;
  outline: 2px solid rgba(37, 99, 235, 0.16);
}

.input.error {
  border-color: #DC2626;
}

.field-error {
  color: #DC2626;
  font-size: 12px;
  font-weight: 500;
}
```

**状态文案映射**(DB → UI):
| DB 值 | UI 展示 |
|---|---|
| active | 活跃 |
| archived | 已归档 |
| success | 成功 |
| failed | 失败 |
| pending | **待观察**(不用"待定",研发语境下"待观察"更自然) |

### 8.5 表格

```css
th {
  background: #F6F8FA;
  color: #57606A;
  font-weight: 600;
}

td, th {
  padding: 10px 12px;
  border-bottom: 1px solid #D8DEE4;
  line-height: 1.4;
}

/* 最后一行不需要底边框 */
tbody tr:last-child td {
  border-bottom: none;
}

.table-row:hover {
  background: #F6F8FA;
  cursor: pointer;
}
```

### 8.6 成功率进度条 + 数字

**同时显示**数字百分比 + 小型进度条 + 颜色等级,避免只靠颜色判断。

```text
75%  ███████░░░
```

| 成功率 | 颜色 | 含义 |
|---:|---|---|
| 80%-100% | 绿色 | 表现好 |
| 50%-79% | 蓝色 | 可继续验证 |
| 1%-49% | 橙色 | 风险较高 |
| 0% | 红色/灰红 | 未成功 |
| 无数据 | 灰色 | 暂无结论 |

**特殊处理**:低样本(1-2 次)成功率不要过度强调,加注"试验次数较少":
```text
100% · 1 次试验
```

**默认排序**:`成功率 DESC, 试验次数 DESC`

**成功率汇总表建议列**:`成功率 | 试验次数 | 成功次数`

### 8.7 Hash 呈现

Hash 是技术字段,不应抢视觉主导。

**列表中**截断显示:
```text
a1b2c3d4…g7h8
```

**详情页**完整显示 + 复制按钮:
```text
a1b2c3d4e5f6g7h8  [复制]
```

```css
.hash {
  font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
  font-size: 12px;
  background: #F6F8FA;
  border: 1px solid #D8DEE4;
  border-radius: 4px;
  padding: 2px 6px;
}
```

### 8.8 Toast(轻量提示)

**位置**:右上角,顶栏下方

**持续**:2.5 秒自动消失

**类型**:
- **Success**:浅绿底,深绿字(如"已保存产品 P001")
- **Error**:浅红底,深红字(如"保存失败:品号 P001 已存在")

### 8.9 Tabs

```css
.tab.active {
  color: #2563EB;
  border-bottom: 2px solid #2563EB;
}
```

- 当前 Tab 蓝色文字 + 下边框蓝色
- 不用大按钮式 Tab

### 8.10 分页

放表格底部右侧。MVP 简化版:
```text
共 128 条  每页 20 条  < 1 2 3 4 >
```

---

## 9. 交互细节

### 9.1 表格行点击

- **整行可点击进入详情**(研发人员浏览时更自然,减少精确瞄准按钮成本)
- 品号/配方名称做成蓝色链接(两种习惯都兼容)
- 操作列提供"查看"按钮
- 操作列点击时阻止行跳转

### 9.2 筛选器

筛选器采用"面板 + 字段网格"结构,避免控件在一行里挤压或自然换行后失序。

**桌面端布局**:
```text
┌────────────────────────────────────────────────────────────┐
│ 筛选标题                                      结果数量     │
├────────────────────────────────────────────────────────────┤
│ 字段标签      字段标签       字段标签/范围组合              │
│ [输入/下拉]   [输入/下拉]    [起始] 至 [结束]               │
│                                      [查询] [重置]         │
└────────────────────────────────────────────────────────────┘
```

**移动端布局**:
- 筛选面板单列堆叠,每个字段宽度 100%。
- 日期范围、成功率区间、排序组合在窄屏拆成纵向控件,避免横向溢出。
- 查询和重置按钮在底部并排;更窄屏幕保持等宽。

**搜索行为**:
- 输入后按 Enter / 点击查询按钮触发
- MVP 简单起见,不做 debounce 自动搜索
- 产品列表:搜索品号/品名 + 状态(活跃/已归档/全部)
- 产品详情配方列表:状态 + 原料/辅料 + 日期范围 + 成功率范围 + 排序
- 成功率查询页:产品 + 日期范围 + 结果状态 + 成功率范围 + 最少试验次数 + 排序

### 9.3 表单校验(行内提示,不用弹窗)

弹窗会打断录入节奏,改用行内提示 + 红色边框。

**校验时机**:
| 场景 | 提示时机 |
|---|---|
| 必填为空 | 提交时提示 |
| 品号重复 | 提交后服务端返回,行内提示 |
| 数量不是数字 | blur 或提交时提示 |
| 原料/辅料行缺字段 | 提交时定位到对应行 |

**表单顶部可加摘要**(错误较多时):
```text
请修正 3 个字段后再保存。
```
(不替代行内提示)

### 9.4 保存成功反馈

- 跳转到详情页
- 顶部 toast `已保存产品 P001` / `已保存配方`
- 2.5 秒后消失

### 9.5 删除/归档确认(分两种)

**产品归档**(软删除,可恢复):
- 二次确认弹窗:
  ```text
  确认归档产品 P001?
  归档后默认不会在产品列表显示,但可以从"已归档"中恢复。
  [取消] [确认归档]
  ```
- 归档不是不可恢复,弹窗不用太重

**产品恢复**:无需二次确认,直接恢复 + toast

**配方删除**(真删,不可恢复):
- 必须二次确认,按钮用 Danger 样式:
  ```text
  确认删除这条配方记录?
  删除后不可恢复。建议仅删除误录入数据。
  [取消] [确认删除]
  ```

**MVP 不做 Undo**(简化,二次确认更可靠)

### 9.6 动态原料/辅料表格

- 默认至少 1 行原料
- "添加行"放表格右上
- 每行最后一列"删除"
- 删除行无需二次确认
- 保存时:
  - 忽略完全空白行
  - 部分填用的行提示错误(行号 + 字段名)

---

## 10. 状态反馈(空/加载/错误)

### 10.1 空状态(统一结构)

格式:`图标/符号 + 引导文案 + 主按钮`

| 场景 | 文案 | 主按钮 |
|---|---|---|
| 产品列表无数据 | 暂无产品<br>还没有录入任何产品... | [+ 新建产品] |
| 搜索无结果 | 没有找到匹配的产品<br>请尝试更换关键词... | [清空筛选] |
| 产品详情无配方 | 暂无配方记录<br>这个产品还没有试验记录... | [+ 新建配方] |
| 成功率无数据 | 暂无成功率数据<br>至少需要一条"成功"或"失败"的配方记录... | (无按钮) |

**注意**:不用复杂插画,简单符号 + 文字 + 按钮。

### 10.2 加载状态

由于本地 FastAPI + SQLite,大多数操作很快。**不做复杂骨架屏**。

- 表格加载:`正在加载产品……`
- 按钮提交:按钮进入 loading 状态 `保存中……`,禁用防重复提交

### 10.3 错误状态(分三类)

| 类型 | 场景 | 呈现方式 |
|---|---|---|
| 字段错误 | 必填/格式 | 行内提示 |
| 操作失败 | 保存/删除失败 | Toast 或页面顶部 alert |
| 严重错误 | 数据库读取/备份失败 | 页面内错误卡片 |

**错误色**:
```css
background: #FEF2F2;
border: 1px solid #FECACA;
color: #B91C1C;
```

### 10.4 备份/导出反馈(用户会担心数据安全)

**备份成功**:`备份完成:kitchen_20260605_183000.zip` + `[打开备份目录]`(可选)
**备份失败**:`备份失败:无法写入 backups 目录。请检查文件夹权限后重试。`
**导出成功**:`Excel 已导出:products_20260605_183000.xlsx`
**导出失败**:`导出失败:...`

---

## 11. 桌面窗适配

### 11.1 默认窗口

推荐:
```text
1024 × 768(够用)
1100 × 760(更舒服,可选)
```

允许缩放:
```text
min_width: 900
min_height: 640
resizable: true
```

### 11.2 窄屏处理(< 800px)

**MVP 简化方案**:表格横向滚动(`overflow-x: auto`),不做卡片式窄屏布局。

**轻度压缩**(800-899px):隐藏低优先级列
- 产品列表可隐藏:创建时间
- 配方列表可隐藏:创建人、创建时间

**重度压缩**(< 800px):表格横向滚动

### 11.3 高度适配

- 768px 高度下,新建配方页最容易出问题
- 不固定底部按钮栏
- 页面自然滚动(`body { overflow-y: auto; }`)
- 表单分块,块间距不要太大

---

## 12. API 设计(FastAPI 路由)

```
POST   /api/login                       # 登录(用户名)
POST   /api/logout                      # 登出

GET    /api/products                    # 列表(支持 ?q=&status=&page=)
POST   /api/products                    # 新建
GET    /api/products/{id}               # 详情
PUT    /api/products/{id}               # 编辑
POST   /api/products/{id}/archive       # 归档
POST   /api/products/{id}/restore       # 恢复归档

GET    /api/recipes                     # 列表(支持 ?product_id=&status=&date_from=&date_to=)
POST   /api/recipes                     # 新建(自动算 hash)
GET    /api/recipes/{id}                # 详情(含原料/辅料)
PUT    /api/recipes/{id}                # 编辑
DELETE /api/recipes/{id}                # 真删

GET    /api/products/{id}/success-rate  # 配方组合成功率汇总
GET    /api/success-rate                # 全局成功率查询(独立页)

POST   /api/backup                      # 一键备份(返回下载链接)
GET    /api/export/excel?type=...       # Excel 导出
GET    /api/export/json                 # JSON 全量导出
```

**返回格式**:统一 `{"ok": true/false, "data": ..., "error": "..."}`

---

## 13. 非功能性需求

### 13.1 备份策略
- 用户手动触发(`backups/kitchen_YYYYMMDD_HHMMSS.zip`)
- 保留最近 10 个,超出删最老的
- (后续)可加每日自动备份

### 13.2 迁移方案
- **A 电脑 → B 电脑**:复制整个 `kitchen-lab-kb/` 文件夹
- B 电脑执行:
  ```bash
  pip install -r requirements.txt
  python startup.py
  ```
- 数据库:`data/kitchen.db` 跟着文件夹走

### 13.3 性能考虑
- 配方表预计 1k-10k 条(每年试验量),SQLite 完全够用
- 关键查询都建了索引(见 §3.2)
- Excel 导出:同步,数据量大时可能 1-2s(可接受)

### 13.4 安全
- MVP 不连公网,本地数据库
- 用户名只做记录,不做密码
- `.env` / `.db` / `backups/` 都在 `.gitignore`

---

## 14. 后续扩展位(数据模型先留好)

| 扩展点 | 当前预留 | 后续要做 |
|---|---|---|
| 多人协作 | `products.created_by` / `recipes.created_by` / `user_logins` 表 | 多人同步/中央 db/冲突合并 |
| 审计日志 | `user_logins` 表(谁在什么时候登录) | 完整 CRUD 审计表(谁/什么时候/改了什么) |
| 权限分级 | 无 | 用户表 + 角色字段 + 权限矩阵 |
| 配方附件 | 无 | `attachments` 表(图片/PDF 存路径) |
| 库存自动累计 | `products.当前数量` 冗余 | 触发器/事务:配方成功 → 自动扣减 |
| 常用原料辅料 | 无 | `materials_dict` 表 + 下拉选择 |
| 数据加密 | 无 | db 加密(sqlcipher) |

---

## 15. MVP 实施优先级

### P0 — 必做(核心可用)
- 顶栏 + 全局布局
- 产品列表表格
- 产品详情页 Tabs
- 新建/编辑产品表单
- 新建/编辑配方表单(分 3 块)
- 状态 Badge(5 种状态)
- 行内表单校验
- 删除/归档二次确认
- Toast 提示
- 空状态(4 种)

### P1 — 建议做(好用)
- 成功率进度条 + 数字
- Hash 复制按钮
- 表格横向滚动(窄屏)
- 备份成功显示文件名
- 导出下拉菜单
- 登录页"上次登录"显示

### P2 — 后续做(锦上添花)
- 更高级筛选(成功率范围/排序)
- 卡片式窄屏布局
- 操作下拉菜单
- 登录记录页面
- 常用原料辅料 datalist 增强
- 附件支持
- 每日自动备份
- 审计日志

---

## 16. 关键设计决策清单

| 决策 | 决定 | 理由 |
|---|---|---|
| 技术栈 | B 路径(PyWebView+FastAPI+SQLite) | 4 个依赖,可接受 |
| 登录形式 | 只记用户名,不做密码 | MVP 1 人,后续扩展 |
| 配方迭代 | 每次新增一条 | 简单,易查询历史 |
| 数量 | 只记"用了多少" | 用户明确 |
| Excel 导出 | 重要,优先做 | 用户明确 |
| 一键备份 | 重要,优先做 | 用户明确 |
| Git | 暂不上,加 .gitignore | 用户明确 |
| 归档 | 软删除 | 可恢复 |
| 配方成功率 | 配方组合 hash + 视图 | 自动识别"同一配方" |
| 侧边栏 | 不要 | 模块少,顶部导航足够 |
| 详情页 Tabs | 是(配方记录/成功率) | 信息分层清晰 |
| 新建配方 | 必须分 3 块 | 动态表格不分块会乱 |
| Undo | 不做 | 二次确认更可靠 |
| 弹窗 vs 行内校验 | 行内优先 | 不打断录入 |
| Hash 显示 | 列表截断,详情完整 | 不抢视觉 |
| 成功率显示 | 数字 + 进度条 + 颜色 | 避免只靠颜色 |
| 状态文案 | 中文(待观察不用待定) | 研发语境自然 |
| 默认窗口 | 1024×768,允许缩放 | 桌面窗够用 |
| 窄屏适配 | 表格横向滚动 | 简化优先 |

---

## 17. 附录

### 17.1 文件结构(实施时)

```
D:\Qwenclaw\wangcai\projects\kitchen-lab-kb\
├── DESIGN.md              # v1 设计文档(过程记录)
├── DESIGN-v2.md           # 本定稿(自包含)
├── README.md              # 部署/迁移/启动(实施时写)
├── app.py                 # FastAPI 入口
├── db.py                  # SQLite 操作
├── backup.py              # 一键备份
├── export.py              # Excel / JSON 导出
├── auth.py                # 简单登录
├── startup.py             # 启动 PyWebView 壳
├── requirements.txt       # 4 个依赖
├── .gitignore             # *.db, __pycache__, backups/
├── data/
│   └── kitchen.db         # SQLite(运行自动建)
├── templates/             # HTML 模板(string.Template)
│   ├── base.html
│   ├── login.html
│   ├── products.html
│   ├── product_new.html
│   ├── product_detail.html
│   ├── recipe_new.html
│   ├── recipe_detail.html
│   └── success_rate.html
└── static/                # CSS / JS
    ├── style.css
    └── app.js
```

### 17.2 用户路径

**常规录入路径**:
```
登录 → 产品列表 → 新建产品 / 选择产品 → 产品详情 → 新建配方 → 保存 → 回到产品详情
```

**查询路径**:
```
产品列表 → 搜索品号/品名 → 产品详情 → 成功率汇总 Tab → 展开 hash → 查看历史试验
```

**备份路径**:
```
任意页面 → 顶栏备份 → 二次确认 → 备份完成提示
```

### 17.3 requirements.txt(参考)

```
fastapi>=0.110
uvicorn[standard]>=0.27
pywebview>=5.0
openpyxl>=3.1
```

### 17.4 一键启动命令(开发参考)

```bash
# 安装依赖
pip install -r requirements.txt

# 开发模式(uvicorn + 浏览器打开 http://127.0.0.1:8000)
uvicorn app:app --reload --port 8000

# 桌面窗模式(双击 startup.py 也行)
python startup.py
```

---

**v2 定稿结束。可以照着这份文档开始实施。** 🦅🐱🔥
  margin-top: 4px;
}

textarea {
  min-height: 88px;
  resize: vertical;  /* 备注字段允许拉伸 */
}
```

### 8.2 按钮(5 个等级)

| 类型 | 用途 | 样式 |
|---|---|---|
| Primary | 保存、新建、查询 | 蓝底白字 |
| Secondary | 取消、导出、备份 | 白底灰边 |
| Text | 查看、编辑 | 蓝色文字 |
| Danger | 删除 | 红色 |
| Subtle Danger | 归档 | 灰色或红色文字,不用红底 |

```css
button[disabled] {
  opacity: 0.6;
  cursor: not-allowed;
}
```

### 8.3 卡片

```css
.card {
  background: #FFFFFF;
  border: 1px solid #D8DEE4;
  border-radius: 8px;
  padding: 16px;
}
```

### 8.4 状态 Badge(统一胶囊徽章)

```css
.badge {
  display: inline-flex;
  align-items: center;
  height: 22px;
  padding: 0 8px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 500;
}
```

**状态文案映射**(DB → UI):
| DB 值 | UI 展示 |
|---|---|
| active | 活跃 |
| archived | 已归档 |
| success | 成功 |
| failed | 失败 |
| pending | **待观察**(不用"待定",研发语境下"待观察"更自然) |

### 8.5 表格

```css
th {
  background: #F6F8FA;
  color: #57606A;
  font-weight: 600;
}

td, th {
  padding: 10px 12px;
  border-bottom: 1px solid #D8DEE4;
  line-height: 1.4;
}

/* 最后一行不需要底边框 */
tbody tr:last-child td {
  border-bottom: none;
}

.table-row:hover {
  background: #F6F8FA;
  cursor: pointer;
}
```

### 8.6 成功率进度条 + 数字

**同时显示**数字百分比 + 小型进度条 + 颜色等级,避免只靠颜色判断。

```text
75%  ███████░░░
```

| 成功率 | 颜色 | 含义 |
|---:|---|---|
| 80%-100% | 绿色 | 表现好 |
| 50%-79% | 蓝色 | 可继续验证 |
| 1%-49% | 橙色 | 风险较高 |
| 0% | 红色/灰红 | 未成功 |
| 无数据 | 灰色 | 暂无结论 |

**特殊处理**:低样本(1-2 次)成功率不要过度强调,加注"试验次数较少":
```text
100% · 1 次试验
```

**默认排序**:`成功率 DESC, 试验次数 DESC`

**成功率汇总表建议列**:`成功率 | 试验次数 | 成功次数`

### 8.7 Hash 呈现

Hash 是技术字段,不应抢视觉主导。

**列表中**截断显示:
```text
a1b2c3d4…g7h8
```

**详情页**完整显示 + 复制按钮:
```text
a1b2c3d4e5f6g7h8  [复制]
```

```css
.hash {
  font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
  font-size: 12px;
  background: #F6F8FA;
  border: 1px solid #D8DEE4;
  border-radius: 4px;
  padding: 2px 6px;
}
```

### 8.8 Toast(轻量提示)

**位置**:右上角,顶栏下方

**持续**:2.5 秒自动消失

**类型**:
- **Success**:浅绿底,深绿字(如"已保存产品 P001")
- **Error**:浅红底,深红字(如"保存失败:品号 P001 已存在")

### 8.9 Tabs

```css
.tab.active {
  color: #2563EB;
  border-bottom: 2px solid #2563EB;
}
```

- 当前 Tab 蓝色文字 + 下边框蓝色
- 不用大按钮式 Tab

### 8.10 分页

放表格底部右侧。MVP 简化版:
```text
共 128 条  每页 20 条  < 1 2 3 4 >
```

---

## 9. 交互细节

### 9.1 表格行点击

- **整行可点击进入详情**(研发人员浏览时更自然,减少精确瞄准按钮成本)
- 品号/配方名称做成蓝色链接(两种习惯都兼容)
- 操作列提供"查看"按钮
- 操作列点击时阻止行跳转

### 9.2 筛选器

横向排列:
```text
[搜索框                    ] [状态 v] [日期范围] [重置]
```

**搜索行为**:
- 输入后按 Enter / 点击查询按钮触发
- MVP 简单起见,不做 debounce 自动搜索
- 产品列表:搜索品号/品名 + 状态(活跃/已归档/全部)
- 配方列表:状态 + 日期范围 + 产品

### 9.3 表单校验(行内提示,不用弹窗)

弹窗会打断录入节奏,改用行内提示 + 红色边框。

**校验时机**:
| 场景 | 提示时机 |
|---|---|
| 必填为空 | 提交时提示 |
| 品号重复 | 提交后服务端返回,行内提示 |
| 数量不是数字 | blur 或提交时提示 |
| 原料/辅料行缺字段 | 提交时定位到对应行 |

**表单顶部可加摘要**(错误较多时):
```text
请修正 3 个字段后再保存。
```
(不替代行内提示)

### 9.4 保存成功反馈

- 跳转到详情页
- 顶部 toast `已保存产品 P001` / `已保存配方`
- 2.5 秒后消失

### 9.5 删除/归档确认(分两种)

**产品归档**(软删除,可恢复):
- 二次确认弹窗:
  ```text
  确认归档产品 P001?
  归档后默认不会在产品列表显示,但可以从"已归档"中恢复。
  [取消] [确认归档]
  ```
- 归档不是不可恢复,弹窗不用太重

**产品恢复**:无需二次确认,直接恢复 + toast

**配方删除**(真删,不可恢复):
- 必须二次确认,按钮用 Danger 样式:
  ```text
  确认删除这条配方记录?
  删除后不可恢复。建议仅删除误录入数据。
  [取消] [确认删除]
  ```

**MVP 不做 Undo**(简化,二次确认更可靠)

### 9.6 动态原料/辅料表格

- 默认至少 1 行原料
- "添加行"放表格右上
- 每行最后一列"删除"
- 删除行无需二次确认
- 保存时:
  - 忽略完全空白行
  - 部分填用的行提示错误(行号 + 字段名)

---

## 10. 状态反馈(空/加载/错误)

### 10.1 空状态(统一结构)

格式:`图标/符号 + 引导文案 + 主按钮`

| 场景 | 文案 | 主按钮 |
|---|---|---|
| 产品列表无数据 | 暂无产品<br>还没有录入任何产品... | [+ 新建产品] |
| 搜索无结果 | 没有找到匹配的产品<br>请尝试更换关键词... | [清空筛选] |
| 产品详情无配方 | 暂无配方记录<br>这个产品还没有试验记录... | [+ 新建配方] |
| 成功率无数据 | 暂无成功率数据<br>至少需要一条"成功"或"失败"的配方记录... | (无按钮) |

**注意**:不用复杂插画,简单符号 + 文字 + 按钮。

### 10.2 加载状态

由于本地 FastAPI + SQLite,大多数操作很快。**不做复杂骨架屏**。

- 表格加载:`正在加载产品……`
- 按钮提交:按钮进入 loading 状态 `保存中……`,禁用防重复提交

### 10.3 错误状态(分三类)

| 类型 | 场景 | 呈现方式 |
|---|---|---|
| 字段错误 | 必填/格式 | 行内提示 |
| 操作失败 | 保存/删除失败 | Toast 或页面顶部 alert |
| 严重错误 | 数据库读取/备份失败 | 页面内错误卡片 |

**错误色**:
```css
background: #FEF2F2;
border: 1px solid #FECACA;
color: #B91C1C;
```

### 10.4 备份/导出反馈(用户会担心数据安全)

**备份成功**:`备份完成:kitchen_20260605_183000.zip` + `[打开备份目录]`(可选)
**备份失败**:`备份失败:无法写入 backups 目录。请检查文件夹权限后重试。`
**导出成功**:`Excel 已导出:products_20260605_183000.xlsx`
**导出失败**:`导出失败:...`

---

## 11. 桌面窗适配

### 11.1 默认窗口

推荐:
```text
1024 × 768(够用)
1100 × 760(更舒服,可选)
```

允许缩放:
```text
min_width: 900
min_height: 640
resizable: true
```

### 11.2 窄屏处理(< 800px)

**MVP 简化方案**:表格横向滚动(`overflow-x: auto`),不做卡片式窄屏布局。

**轻度压缩**(800-899px):隐藏低优先级列
- 产品列表可隐藏:创建时间
- 配方列表可隐藏:创建人、创建时间

**重度压缩**(< 800px):表格横向滚动

### 11.3 高度适配

- 768px 高度下,新建配方页最容易出问题
- 不固定底部按钮栏
- 页面自然滚动(`body { overflow-y: auto; }`)
- 表单分块,块间距不要太大

---

## 12. API 设计(FastAPI 路由)

```
POST   /api/login                       # 登录(用户名)
POST   /api/logout                      # 登出

GET    /api/products                    # 列表(支持 ?q=&status=&page=)
POST   /api/products                    # 新建
GET    /api/products/{id}               # 详情
PUT    /api/products/{id}               # 编辑
POST   /api/products/{id}/archive       # 归档
POST   /api/products/{id}/restore       # 恢复归档

GET    /api/recipes                     # 列表(支持 ?product_id=&status=&date_from=&date_to=)
POST   /api/recipes                     # 新建(自动算 hash)
GET    /api/recipes/{id}                # 详情(含原料/辅料)
PUT    /api/recipes/{id}                # 编辑
DELETE /api/recipes/{id}                # 真删

GET    /api/products/{id}/success-rate  # 配方组合成功率汇总
GET    /api/success-rate                # 全局成功率查询(独立页)

POST   /api/backup                      # 一键备份(返回下载链接)
GET    /api/export/excel?type=...       # Excel 导出
GET    /api/export/json                 # JSON 全量导出
```

**返回格式**:统一 `{"ok": true/false, "data": ..., "error": "..."}`

---

## 13. 非功能性需求

### 13.1 备份策略
- 用户手动触发(`backups/kitchen_YYYYMMDD_HHMMSS.zip`)
- 保留最近 10 个,超出删最老的
- (后续)可加每日自动备份

### 13.2 迁移方案
- **A 电脑 → B 电脑**:复制整个 `kitchen-lab-kb/` 文件夹
- B 电脑执行:
  ```bash
  pip install -r requirements.txt
  python startup.py
  ```
- 数据库:`data/kitchen.db` 跟着文件夹走

### 13.3 性能考虑
- 配方表预计 1k-10k 条(每年试验量),SQLite 完全够用
- 关键查询都建了索引(见 §3.2)
- Excel 导出:同步,数据量大时可能 1-2s(可接受)

### 13.4 安全
- MVP 不连公网,本地数据库
- 用户名只做记录,不做密码
- `.env` / `.db` / `backups/` 都在 `.gitignore`

---

## 14. 后续扩展位(数据模型先留好)

| 扩展点 | 当前预留 | 后续要做 |
|---|---|---|
| 多人协作 | `products.created_by` / `recipes.created_by` / `user_logins` 表 | 多人同步/中央 db/冲突合并 |
| 审计日志 | `user_logins` 表(谁在什么时候登录) | 完整 CRUD 审计表(谁/什么时候/改了什么) |
| 权限分级 | 无 | 用户表 + 角色字段 + 权限矩阵 |
| 配方附件 | 无 | `attachments` 表(图片/PDF 存路径) |
| 库存自动累计 | `products.当前数量` 冗余 | 触发器/事务:配方成功 → 自动扣减 |
| 常用原料辅料 | 无 | `materials_dict` 表 + 下拉选择 |
| 数据加密 | 无 | db 加密(sqlcipher) |

---

## 15. MVP 实施优先级

### P0 — 必做(核心可用)
- 顶栏 + 全局布局
- 产品列表表格
- 产品详情页 Tabs
- 新建/编辑产品表单
- 新建/编辑配方表单(分 3 块)
- 状态 Badge(5 种状态)
- 行内表单校验
- 删除/归档二次确认
- Toast 提示
- 空状态(4 种)

### P1 — 建议做(好用)
- 成功率进度条 + 数字
- Hash 复制按钮
- 表格横向滚动(窄屏)
- 备份成功显示文件名
- 导出下拉菜单
- 登录页"上次登录"显示

### P2 — 后续做(锦上添花)
- 更高级筛选(成功率范围/排序)
- 卡片式窄屏布局
- 操作下拉菜单
- 登录记录页面
- 常用原料辅料 datalist 增强
- 附件支持
- 每日自动备份
- 审计日志

---

## 16. 关键设计决策清单

| 决策 | 决定 | 理由 |
|---|---|---|
| 技术栈 | B 路径(PyWebView+FastAPI+SQLite) | 4 个依赖,可接受 |
| 登录形式 | 只记用户名,不做密码 | MVP 1 人,后续扩展 |
| 配方迭代 | 每次新增一条 | 简单,易查询历史 |
| 数量 | 只记"用了多少" | 用户明确 |
| Excel 导出 | 重要,优先做 | 用户明确 |
| 一键备份 | 重要,优先做 | 用户明确 |
| Git | 暂不上,加 .gitignore | 用户明确 |
| 归档 | 软删除 | 可恢复 |
| 配方成功率 | 配方组合 hash + 视图 | 自动识别"同一配方" |
| 侧边栏 | 不要 | 模块少,顶部导航足够 |
| 详情页 Tabs | 是(配方记录/成功率) | 信息分层清晰 |
| 新建配方 | 必须分 3 块 | 动态表格不分块会乱 |
| Undo | 不做 | 二次确认更可靠 |
| 弹窗 vs 行内校验 | 行内优先 | 不打断录入 |
| Hash 显示 | 列表截断,详情完整 | 不抢视觉 |
| 成功率显示 | 数字 + 进度条 + 颜色 | 避免只靠颜色 |
| 状态文案 | 中文(待观察不用待定) | 研发语境自然 |
| 默认窗口 | 1024×768,允许缩放 | 桌面窗够用 |
| 窄屏适配 | 表格横向滚动 | 简化优先 |

---

## 17. 附录

### 17.1 文件结构(实施时)

```
D:\Qwenclaw\wangcai\projects\kitchen-lab-kb\
├── DESIGN.md              # v1 设计文档(过程记录)
├── DESIGN-v2.md           # 本定稿(自包含)
├── README.md              # 部署/迁移/启动(实施时写)
├── app.py                 # FastAPI 入口
├── db.py                  # SQLite 操作
├── backup.py              # 一键备份
├── export.py              # Excel / JSON 导出
├── auth.py                # 简单登录
├── startup.py             # 启动 PyWebView 壳
├── requirements.txt       # 4 个依赖
├── .gitignore             # *.db, __pycache__, backups/
├── data/
│   └── kitchen.db         # SQLite(运行自动建)
├── templates/             # HTML 模板(string.Template)
│   ├── base.html
│   ├── login.html
│   ├── products.html
│   ├── product_new.html
│   ├── product_detail.html
│   ├── recipe_new.html
│   ├── recipe_detail.html
│   └── success_rate.html
└── static/                # CSS / JS
    ├── style.css
    └── app.js
```

### 17.2 用户路径

**常规录入路径**:
```
登录 → 产品列表 → 新建产品 / 选择产品 → 产品详情 → 新建配方 → 保存 → 回到产品详情
```

**查询路径**:
```
产品列表 → 搜索品号/品名 → 产品详情 → 成功率汇总 Tab → 展开 hash → 查看历史试验
```

**备份路径**:
```
任意页面 → 顶栏备份 → 二次确认 → 备份完成提示
```

### 17.3 requirements.txt(参考)

```
fastapi>=0.110
uvicorn[standard]>=0.27
pywebview>=5.0
openpyxl>=3.1
```

### 17.4 一键启动命令(开发参考)

```bash
# 安装依赖
pip install -r requirements.txt

# 开发模式(uvicorn + 浏览器打开 http://127.0.0.1:8000)
uvicorn app:app --reload --port 8000

# 桌面窗模式(双击 startup.py 也行)
python startup.py
```

---

**v2 定稿结束。可以照着这份文档开始实施。** 🦅🐱🔥
 
